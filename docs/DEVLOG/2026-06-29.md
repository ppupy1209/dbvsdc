# DEVLOG 2026-06-29 — QA 후속 정리 (미래 30년 투영 · 임금피크 잔재 제거)

`FINANCIAL-QA-FIX-RATIONALE.md` + 최신 커밋(`4e38b98` 등) 검토 후 후속 수정. 프리뷰(dev 3100) DOM 측정으로 동작 검증, `tsc --noEmit` 통과.

## 검토 결과 요약

- **백엔드 시드 정합성 OK** — `SampleMarketDataSource.java`, `V2__seed_curated_index_returns.sql`가 프론트 `indexData.ts`와 값 일치. KOSPI200/KOSDAQ150 교체 후에도 front/back 계약 드리프트 없음.
- **nq 1995·1996 동일값(43.34)** — `a73caef`에서 price 42.54에 평균배당 +0.8 실제 가산 확인(라벨 정확). 단 두 해 동일값은 여전히 데이터 검증 필요(이전 주석의 "확인 필요" 메모가 `d2255a6`에서 삭제됨). 프론트·백엔드 동일값이라 검증 후 일괄 수정 필요 → 데이터(Codex) 영역, TODO 등재.

## 수정 1 — 미래 시뮬레이션 30년 투영 (국내 지수)

문제: 기간 슬라이더 상한이 과거/미래 공통으로 지수별 과거 데이터 길이에 묶여 KOSPI200=16년·KOSDAQ150=10년까지만 미래 투영 가능 → 커리어 전체(30년) 시뮬레이션 불가.

- `Simulator.tsx`: `maxPeriod`를 모드별로 분리. 과거=`min(30, 가용연수)`, **미래=30 고정**.
- `calc.ts` `simulate` 미래 분기 분리:
  - **평균 시나리오**: CAGR 반복이라 과거 길이와 무관 → `n = yearsInput`(최대 30) 전체 투영.
  - **최악 시나리오**: 실제 과거 윈도우 필요 → `n = min(yearsInput, 가용연수)`, 반환된 윈도우 길이로 `n` 보정. UI는 기존 `(과거 최악 N년 경로: start~end)` 라벨로 단축 기간 표기.
- 검증: 미래+KOSDAQ150+30년 → 평균 차트 31점(~30년), 최악 차트 11점(~10년), NaN 없음. 과거 백테스트는 KOSDAQ 10·KOSPI 16 제한 유지.
- 결정 근거: 사용자 선택 "미래는 30년 허용(평균만)". 토글 전환 시 평균=30년, 최악=가용기간으로 horizon이 달라지는 것은 의도된 동작.

## 수정 2 — 임금피크 잔재 코드 제거

2026-06-26에 UI에서 임금피크 옵션 제거했으나 `calc.ts`에 계산부만 잔존(DEVLOG 2026-06-26 line 19에 명시). 호출부 없음 확인 후 제거.

- `PEAK_CUT` 상수, `SimOptions.wagePeak`, `if (opts.wagePeak) {...}` 블록 삭제. 동작 변화 없음.
