# DEVLOG 2026-07-01 — 미래 Monte Carlo 확률 밴드 추가 → 사용자 판단으로 제거

## 요약

미래 시뮬레이션에 **Monte Carlo 확률 밴드**를 구현·검증했으나, **사용자 판단("확률 밴드 개념이
일반 사용자에게 어렵다")에 따라 같은 세션에서 제거**함. 코드는 원상복구(revert), 결정만 기록.

## 무엇을 만들었었나 (참고용 — 되돌림)

- `calc.ts monteCarloBands()`: 지수 실제 연도별 수익률을 IID 부트스트랩(1,500경로, mulberry32 고정시드=결정적),
  안전30/위험70+실부담비용 블렌딩 → p10/25/50/75/90 잔액 경로 + `probDcWins` + 종말 10~90% 범위 + 중앙값 CAGR.
- `simulate()` `"band"` 분기(중앙값 경로가 기존 verdict/세금 UI 구동, 이력<5년 평균 폴백).
- UI: 미래 3버튼(평균/확률 밴드/최악, 밴드 기본), 불확실성 콘(10~90%+25~75% 띠)+중앙값선,
  "N% 확률로 DC 우위 · 10~90% A~B", 방법 캡션, 호버 3줄, `scn=band` 공유.
- 검증: tsc 클린 · vitest 42/42 · build · 프리뷰 E2E(밴드 렌더·86% 확률·무회귀·모바일·다크) 통과했었음.

## 왜 뺐나 (결정)

- **UX 우선**: 백분위 띠/부트스트랩은 통계 개념이라 일반 퇴직연금 사용자에겐 진입장벽. 사이트 톤(누구나 30초)과 안 맞음.
- 커밋 하나(`7efba7c`)로 격리돼 있어 `git revert`로 깨끗하게 원복. 미래 모드는 **평균/최악 2종(기본 최악)**으로 복귀.

## 현재 상태 (되돌린 뒤)

- 코드: 밴드 관련 전부 제거. `FutureScenario = "average" | "worst"`, `SimResult.band` 없음, tsc 클린, **vitest 35/35**, build 통과.
- 프리뷰(:3100): 미래 시나리오 버튼 평균/최악만, 기본 최악, 콘솔 오류 0. 밴드 잔재 없음.
- ⚠️ **재오픈된 이슈**: REVIEW Top10 **#4(미래 '평균' 직선 = 확정수익 오인)**는 다시 "경고 배너 + 과거빈도%"
  수준의 완화만 남음. 다음에 다룬다면 **통계 개념 없이** 쉬운 표현(예: "미래는 해마다 크게 오르내려 확정이
  아닙니다" + 단순 좋음/나쁨 범위)으로 접근할 것.

## 다음 세션 후보 (변동 없음)

- (정확성) 환율 연말종가(BOK ECOS 731Y001) — ECOS 인증키 필요.
- (기능) 개인 IRP 추가납입 과세 분리(세액공제·기타소득세 16.5%·연금소득세).
- (배포) AWS EC2 프로비저닝 + 도메인 + Vercel — 사용자 액션.
